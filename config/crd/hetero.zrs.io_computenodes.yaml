---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: computenodes.hetero.zrs.io
spec:
  group: hetero.zrs.io
  names:
    kind: ComputeNode
    listKind: ComputeNodeList
    plural: computenodes
    shortNames:
    - cn
    singular: computenode
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.vendor
      name: Vendor
      type: string
    - jsonPath: .spec.nodeName
      name: Node
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.totalCapacity.vram
      name: VRAM
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ComputeNode is the Schema for the computenodes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ComputeNodeSpec defines the desired state of ComputeNode
            properties:
              nodeName:
                description: NodeName is the Kubernetes node name
                type: string
              totalCapacity:
                description: TotalCapacity is the total compute capacity
                properties:
                  fp16Tflops:
                    description: FP16 TFLOPS
                    format: int64
                    type: integer
                  fp32Tflops:
                    description: FP32 TFLOPS
                    format: int64
                    type: integer
                  vram:
                    description: VRAM in bytes
                    format: int64
                    type: integer
                required:
                - vram
                type: object
              vendor:
                description: Vendor is the hardware vendor (nvidia, huawei, hygon,
                  cambricon)
                type: string
            required:
            - nodeName
            - totalCapacity
            - vendor
            type: object
          status:
            description: ComputeNodeStatus defines the observed state of ComputeNode
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: ComputeNodeCondition describes the state of a compute
                    node at a certain point
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned
                      format: date-time
                      type: string
                    message:
                      description: Message is a human-readable message indicating
                        details
                      type: string
                    reason:
                      description: Reason is the reason for the condition's last transition
                      type: string
                    status:
                      description: Status of the condition
                      type: string
                    type:
                      description: Type of condition
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              devices:
                description: Devices is the list of compute devices
                items:
                  description: DeviceInfo represents a single compute device
                  properties:
                    healthScore:
                      description: HealthScore is the health score (0-100)
                      type: number
                    id:
                      description: ID is the device identifier
                      type: string
                    interconnectType:
                      description: InterconnectType is the interconnect type (NVLink,
                        HCCS, PCIe)
                      type: string
                    model:
                      description: Model is the device model name
                      type: string
                    pcieBusID:
                      description: PCIEBusID is the PCI bus ID
                      type: string
                    vramTotal:
                      description: VRAMTotal is the total VRAM in bytes
                      format: int64
                      type: integer
                    vramUsed:
                      description: VRAMUsed is the used VRAM in bytes
                      format: int64
                      type: integer
                  required:
                  - healthScore
                  - id
                  - model
                  - vramTotal
                  - vramUsed
                  type: object
                type: array
              phase:
                description: Phase is the current phase of the compute node
                type: string
            required:
            - phase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
